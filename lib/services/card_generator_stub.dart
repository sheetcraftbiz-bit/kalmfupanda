import '../models/quote_model.dart';

/// Stub implementation for mobile platforms
/// CardGenerator is only available on web
class CardGenerator {
  static void generateAndDownload(Quote quote, String cardType) {
    // No-op on mobile - use native methods instead
  }
}
