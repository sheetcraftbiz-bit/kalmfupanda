/// Stub implementation for mobile platforms
/// WebCardGenerator is only available on web
class WebCardGenerator {
  static void generateShareCard(quote, type) {
    // No-op on mobile - use native methods instead
  }
}

enum ShareCardType { colorful, appleNotes, twitter }
